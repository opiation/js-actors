{
  "$schema": "https://json.schemastore.org/package",
  "name": "js-actors",
  "version": "0.1.0",
  "description": "An experimental actor model implementation in JavaScript for multiple environments",
  "main": "actor.js",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit && echo '✅ Valid according to type checker'",
    "serve-example": "alive-server --host=127.0.0.1",
    "test": "node --test",
    "test-watch": "nodemon --exec 'node --test' --quiet"
  },
  "devDependencies": {
    "alive-server": "1.3.0",
    "chalk": "5.3.0",
    "nodemon": "3.1.0",
    "typescript": "5.4.5"
  },
  "author": "opiation <nick.gelinas@gmail.com>",
  "license": "GPL-3.0"
}
